  {{#each this}}
    <div class=" card">
       {{!-- <div class="card-header"></div> --}}
          <div class="card-body">
             <p class="mb-3">{{text}}</p>
             <p class="mb-3">by {{user.username}}, on {{format_date created_at}}</p>
          </div>
    </div>
  {{/each}}
